1. Move DB initialization out of import time

Do NOT do this at module load:

init_db_pool()
test_connection()
setup_admin_user()


Instead:

@app.before_first_request
def startup_tasks():
    init_db_pool()
    test_connection()


This ensures:

Runs once

Only when app is actually serving

Not on every cold import